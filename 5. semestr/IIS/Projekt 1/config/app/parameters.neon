# Parameters
#
parameters:

	# System
	system:
		error:
			email: dev@dev.dev
			presenter: Front:Error

	# Database
	database:
		driver: mysqli
		port: 3306

	# Project
	project:
		rev: "1.0.0"

	# File
	files:
		root: %appDir%/../data/files

	# Mailing
	mailing:
		from: webapp@localhost
		from_name: Webapp

	# STMP
	smtp:
		host: localhost
		username: fake
		password: fake
